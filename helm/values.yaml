replicaCount: 1

image:
  repository: doronfe/simple-app
  pullPolicy: IfNotPresent
  tag: werkzeug

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  hosts:
    - host: 127.0.0.1.nip.io
      paths:
        - path: /
          pathType: Prefix


resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
